[project]
name = "cw-mcp-server"
version = "0.1.0"
description = "CodeWhale Runtime API → MCP 桥接服务器"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.28.0",
    "httpx-sse>=0.4.0",
]

[project.scripts]
cw-mcp-server = "server:main"
